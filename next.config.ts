import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com", "api.microlink.io"], // Add Unsplash domain here
  },
  /* config options here */
};

export default nextConfig;
